# Q2438 Windows reproduction

This public repository contains one Kubernetes manifest authoring task and its Windows Server 2025 workflow.

The workflow uses the native Windows kubectl client and its built-in Kustomize support. It does not connect to a Kubernetes API Server.
